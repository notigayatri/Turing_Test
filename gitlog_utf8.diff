commit 73bb3439169b61d63c8e51d0c0ccbbc9cb990c4e
Author: notigayatri <gayatrin005@gmail.com>
Date:   Tue Mar 31 11:32:53 2026 +0530

    db modification

diff --git a/server/db_out.json b/server/db_out.json
new file mode 100644
index 0000000..971fd8c
--- /dev/null
+++ b/server/db_out.json
@@ -0,0 +1,29 @@
+{
+  "q": {
+    "_id": "69c97a7e45e2d7143ed8a990",
+    "title": "Refactor logic for handling cumulative values",
+    "description": "Refactored the implementation to improve performance and reduce repeated calculations.\n\nThe updated logic simplifies the process and ensures consistent results across different input scenarios.",
+    "codeSnippet": "def build_prefix_sum(arr):\n    n = len(arr)\n    prefix = [0] * (n + 1)\n    \n    for i in range(n):\n        prefix[i + 1] = prefix[i] + arr[i]\n    \n    return prefix\n\ndef range_sum(prefix, left, right):\n    if left < 0 or right >= len(prefix) - 1 or left > right:\n        return 0\n    \n    return prefix[right + 1] - prefix[left]\n\n\n# Example usage\narr = [2, 4, 1, 5, 3]\nprefix = build_prefix_sum(arr)\n\nprint(range_sum(prefix, 1, 3))  # Output should be 10 (4+1+5)",
+    "language": "python",
+    "commitMessage": "",
+    "correctOption": "AI",
+    "answerReasoning": "No reasoning provided.",
+    "purpose": "General code review assessment.",
+    "timerDuration": 60,
+    "order": 1,
+    "__v": 0
+  },
+  "r": {
+    "_id": "69cb5f0a8c5d6b8219526ee1",
+    "questionId": "69c97a7e45e2d7143ed8a990",
+    "teamId": "69cb368c2f00bcca2d194a28",
+    "__v": 0,
+    "confidence": 3,
+    "llmReasoning": null,
+    "llmScore": null,
+    "reasoning": "too perfect and comments",
+    "selection": "AI",
+    "timestamp": "2026-03-31T05:43:59.859Z",
+    "understanding": "optimised prefix sum"
+  }
+}
\ No newline at end of file
diff --git a/server/db_output.txt b/server/db_output.txt
new file mode 100644
index 0000000..49d2bb9
Binary files /dev/null and b/server/db_output.txt differ
diff --git a/server/test_db.js b/server/test_db.js
new file mode 100644
index 0000000..3ffb28e
--- /dev/null
+++ b/server/test_db.js
@@ -0,0 +1,32 @@
+require('dotenv').config();
+const mongoose = require('mongoose');
+const Question = require('./models/Question.js');
+const Response = require('./models/Response.js');
+
+async function testDB() {
+  await mongoose.connect(process.env.MONGODB_URI);
+  console.log('Connected to MongoDB.');
+
+  const q = await Question.findOne().sort({ _id: -1 });
+  if (q) {
+      console.log('--- LATEST QUESTION ---');
+      console.log('Title:', q.title);
+      console.log('Purpose:', q.purpose);
+      console.log('Reasoning:', q.answerReasoning);
+  } else {
+      console.log('No questions found in database.');
+  }
+
+  const r = await Response.findOne().sort({ _id: -1 });
+  if (r) {
+      console.log('--- LATEST RESPONSE ---');
+      console.log('Team:', r.teamId);
+      console.log('LLM Score:', r.llmScore);
+      console.log('LLM Reasoning:', r.llmReasoning);
+  } else {
+      console.log('No responses found in database.');
+  }
+
+  process.exit(0);
+}
+testDB().catch(console.error);
